# Issue #45 渐进式 SQLite 成本索引实施计划

- Plan ID: `P20260902-080535`
- 执行时间(北京时间): `2026-09-02 08:05:35 +0800`
- 项目根目录: `/Users/lzl/FILE/github/codexbar`
- 来源需求: 完成 Issue #45，创建 PR、合并关闭、打包并更新本机

## Plan

# Issue #45 渐进式 SQLite 成本索引实施计划

## 成功标准

- Cost 不再把未知、扫描中或陈旧状态显示为真实零；
- 人工刷新请求不会被在途自动刷新丢弃；
- 只追加 JSONL 的热刷新只读取新增字节；
- Today、30d、Lifetime 从持久聚合快速读取；
- 扫描可断点恢复并显示真实进度；
- 现有计费与 fork/subagent 去重语义不回归；
- Issue #45 通过 PR 合并关闭，本机更新为优化 Release 且无重复 App。

## 执行步骤

1. 为 lost refresh、last-known-good、append cursor、边界半行、截断/替换和聚合窗口编写失败测试。
2. 实现 SQLite WAL store、schema migration、事务封装与只读 snapshot。
3. 抽取可恢复 parser state，实现 per-file byte-offset 扫描、anchor 校验和相关文件局部重建。
4. 实现事件去重、fork lineage/accumulator 持久化以及 file/day 与 global/day 聚合。
5. 将 `LocalCostSummaryService` 接入新索引，保留 legacy JSON fallback 与迁移期对账。
6. 实现 `LocalCostRefreshCoordinator`，合并请求强度、后台预算 catch-up、人工加速和独立进度/错误状态。
7. 更新 Cost 卡片和详情面板，展示 last-good、未知、stale、真实进度、最后扫描时间，并修正 30 个自然日图表。
8. 运行针对性测试、全量测试、静态分析和格式检查；执行独立代码审查并修正高优先级发现。
9. 在真实 14.7 GiB 日志上验证冷 catch-up、热增量、结果对账、CPU/内存和重启续传。
10. 提交中文 Lore commits，推送分支并创建关联 `Fixes #45` 的 PR；通过后合并并确认 issue 关闭。
11. 构建优化 Release 和本地安装包，安装到 `/Applications/codexbar.app`，验证不链接 debug dylib 并清理所有临时副本与 Launch Services 残留。

## 风险控制

- 不删除原始 session JSONL 或认证配置；
- SQLite 建立失败时继续展示旧摘要并允许重试；
- 迁移期保留旧 JSON 作为回退，直到真实数据对账通过；
- 任何 parser/schema 版本变化必须使相关文件安全重建，不能静默复用不兼容 cursor；
- Release 验证必须覆盖 arm64 与 x86_64 优化构建边界。
