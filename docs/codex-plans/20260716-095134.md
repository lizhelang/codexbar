# 菜单栏百分比纵向内嵌

- Plan ID: `P20260716-095134`
- 执行时间(北京时间): `2026-07-16 09:51:34 +0800`
- 项目根目录: `/Users/lzl/FILE/github/codexbar`
- 来源需求: 顶部显示最短实际额度窗口百分比，下方保持动态 5h/7d 进度，整个菜单栏状态项始终为单个方形图标

## Plan

# 菜单栏百分比纵向内嵌实施计划

设计基线：`docs/plans/2026-07-16-vertical-menu-bar-usage-design.md`

1. 先扩充 `MenuBarUsageIconRendererTests` 与 `MenuBarStatusItemPresentationTests`，锁定顶部只取首个实际窗口、底部动态一至两条进度、状态项始终方形且标题为空。
2. 扩展 `MenuBarUsageIconSpec` 与 18×18 Retina 模板渲染器，在保持纯进度坐标不变的同时加入数值顶部、进度底部的像素对齐布局。
3. 调整状态栏展示模型，让 `showsMenuBarUsageText` 只控制图标内数值，不再切换 `variableLength`；异常及供应商系统符号继续保持优先级。
4. 更新中英文设置文案，明确最短实际额度窗口及 5h 缺失时自动显示 7d 的规则，同时保留原持久化键。
5. 运行渲染器与展示模型定向测试，再运行完整测试套件；修复所有本次变更引入的失败。
6. 进行独立代码评审，处理阻断问题并复测。
7. 构建、签名并替换 `/Applications/codexbar.app`，在真实菜单栏开启设置，核对方形尺寸、顶部数值和动态进度条。
8. 清理构建副本及 Launch Services / Spotlight 残留，确认只剩目标安装副本，并按 Lore 协议提交实现。
